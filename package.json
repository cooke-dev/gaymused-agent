{
  "name": "gaymused-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic brain layer over OpenRails for the GASOK program on GIWA: a Telegram copilot that proposes bounded budgets and acts within them.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prove-rail": "tsx src/scripts/prove-rail.ts",
    "print-state": "tsx src/scripts/print-state.ts",
    "try-brain": "tsx src/scripts/try-brain.ts",
    "print-intent": "tsx src/scripts/print-intent.ts",
    "try-handoff": "tsx src/scripts/try-handoff.ts",
    "selftest-handoff": "tsx src/scripts/selftest-handoff.ts",
    "money-shot": "tsx src/scripts/money-shot.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "fastify": "^5.10.0",
    "openrails-sdk": "^0.1.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
